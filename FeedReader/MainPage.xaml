<Page
    x:Class="FeedReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FeedReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Name="ReloadButton" HorizontalAlignment="Center" Content="Reload" Click="ReloadButton_Click" Margin="10"/>
            <TextBox Name="FeedTextBox" Width="150" Margin="10"></TextBox>
            <Button Name="AddFeedButton" Content="+" HorizontalAlignment="Right"  Margin="10" Click="AddFeedButton_Click" />
        </StackPanel>

        <Frame Name="MyFrame" Grid.Row="1"></Frame>
        
    </Grid>
</Page>
